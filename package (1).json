{
  "name": "sapo-proxy",
  "version": "1.0.0",
  "description": "Proxy server for Sapo toddler AI app — keeps API keys secure on Railway",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^3.3.2"
  }
}
